export type ActionElements = ActionElement[];

export type ActionElement = {
    position: number;
    element: React.ElementType;
    headerTitle: string;
}

export type DragAndDropColumn = {
    position: number;
    element: React.ElementType;
    headerTitle: string;
}